# Hand a portal quote to an agent without retyping it

**Assigned:** Dana Whitfield

A customer who stalls online and calls the agency starts from scratch. The
quote exists; the agent just cannot open it.

- Quote reference number surfaced to the customer and searchable by the agent.
- Agent opens the quote at the step it stopped on.
- Attribution rules agreed before launch — whose sale is it
  (policy/auto-policy/quote-policy.feature.md).
